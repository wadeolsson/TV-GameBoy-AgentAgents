allprojects {
    group = "com.tvgameboy"
    version = "0.1.0"
}

subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }

    java {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}
